import { InvestorSortPipe } from './investor-sort.pipe';

describe('InvestorSortPipe', () => {
  it('create an instance', () => {
    const pipe = new InvestorSortPipe();
    expect(pipe).toBeTruthy();
  });
});
